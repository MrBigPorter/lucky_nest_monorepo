# ==========================================
# ⚠️ 此文件已弃用！
# ==========================================
# 环境变量已统一迁移到 deploy/ 目录:
#   - deploy/.env.dev   → 开发环境
#   - deploy/.env.prod  → 生产环境
#   - deploy/.env.example → 模板
#
# 使用方式:
#   开发: docker compose --env-file deploy/.env.dev up -d --build
#   生产: docker compose -f compose.prod.yml --env-file deploy/.env.prod up -d --build
# ==========================================
