# =========================
# 0) 全局 gzip
# =========================
gzip on;
gzip_min_length 1k;
gzip_types application/json text/plain application/javascript text/css application/xml;

# =========================
# 0.2) 专门抓媒体请求的开关
# =========================
map $uri $is_media {
    default 0;
    ~^/uploads/ 1;
    ~^/cdn-cgi/image/ 1;
}

# =========================
#  NEW: 预检请求分流（避免 if 在 location 里乱跳）
# =========================
map $request_method $is_options {
    default 0;
    OPTIONS 1;
}

# =========================
# 0.1) 日志格式（建议放在 http{} 里）
# =========================
log_format joy '$time_iso8601 '
               'media=$is_media '
               'ip=$remote_addr '
               'host=$host '
               'method=$request_method '
               'uri="$uri" '
               'args="$args" '
               'status=$status '
               'bytes=$body_bytes_sent '
               'referer="$http_referer" '
               'origin="$http_origin" '
               'ua="$http_user_agent" '
               'range="$http_range" '
               'cache="$sent_http_cf_cache_status" '
               'ctype="$sent_http_content_type" '
               'upstream="$upstream_addr" '
               'ustatus="$upstream_status" '
               'urt="$upstream_response_time" '
               'rt="$request_time"';

# =========================
# HTTPS Server (443)
# =========================
server {
    listen 443 ssl;
    http2 on;

    server_name admin.joyminis.com dev-api.joyminis.com dev.joyminis.com;

    ssl_certificate     /etc/nginx/certs/admin.joyminis.com+4.pem;
    ssl_certificate_key /etc/nginx/certs/admin.joyminis.com+4-key.pem;

    proxy_ssl_server_name on;

    #  只保留一个 access_log
    access_log /dev/stdout joy;
    error_log  /dev/stderr warn;

    # ----------------------------------------------------------
    # 1) Cloudflare Image Resizing 入口：/cdn-cgi/image/...
    # ----------------------------------------------------------
    location ^~ /cdn-cgi/image/ {

        #  关键：先把上游可能返回的 CORS 头“隐藏”，避免重复
        proxy_hide_header Access-Control-Allow-Origin;
        proxy_hide_header Access-Control-Allow-Methods;
        proxy_hide_header Access-Control-Allow-Headers;
        proxy_hide_header Access-Control-Expose-Headers;

        #  我们自己统一加一套（只会出现一次）
        add_header Access-Control-Allow-Origin "*" always;
        add_header Access-Control-Allow-Methods "GET,HEAD,OPTIONS" always;
        add_header Access-Control-Allow-Headers "*" always;
        add_header Access-Control-Expose-Headers "Content-Length,Content-Range,Accept-Ranges" always;

        #  预检请求直接返回（无 upstream）
        if ($is_options) { return 204; }

        proxy_buffering off;
        proxy_max_temp_file_size 0;

        proxy_pass https://img.joyminis.com;
        proxy_set_header Host img.joyminis.com;
        proxy_ssl_name img.joyminis.com;
    }

    # ----------------------------------------------------------
    # 2) 视频：Range + 不缓冲
    # ----------------------------------------------------------
    location ~* ^/uploads/.*\.(mp4|mov|m4a)$ {

        #  同样：隐藏上游 CORS，自己加一套（避免重复）
        proxy_hide_header Access-Control-Allow-Origin;
        proxy_hide_header Access-Control-Allow-Methods;
        proxy_hide_header Access-Control-Allow-Headers;
        proxy_hide_header Access-Control-Expose-Headers;

        add_header Access-Control-Allow-Origin "*" always;
        add_header Access-Control-Allow-Methods "GET,HEAD,OPTIONS" always;
        add_header Access-Control-Allow-Headers "*" always;
        add_header Access-Control-Expose-Headers "Content-Length,Content-Range,Accept-Ranges" always;

        if ($is_options) { return 204; }

        proxy_buffering off;
        proxy_max_temp_file_size 0;

        proxy_http_version 1.1;
        proxy_set_header Range $http_range;
        proxy_pass_header Content-Range;
        proxy_pass_header Accept-Ranges;

        proxy_pass https://img.joyminis.com;
        proxy_set_header Host img.joyminis.com;
        proxy_ssl_name img.joyminis.com;
    }

    # ----------------------------------------------------------
    # 3) uploads 兜底（图片/附件）
    # ----------------------------------------------------------
    location ^~ /uploads/ {

        #  同样：隐藏上游 CORS，自己加一套（避免重复）
        proxy_hide_header Access-Control-Allow-Origin;
        proxy_hide_header Access-Control-Allow-Methods;
        proxy_hide_header Access-Control-Allow-Headers;
        proxy_hide_header Access-Control-Expose-Headers;

        add_header Access-Control-Allow-Origin "*" always;
        add_header Access-Control-Allow-Methods "GET,HEAD,OPTIONS" always;
        add_header Access-Control-Allow-Headers "*" always;
        add_header Access-Control-Expose-Headers "Content-Length,Content-Range,Accept-Ranges" always;

        if ($is_options) { return 204; }

        proxy_buffering off;
        proxy_max_temp_file_size 0;

        proxy_pass https://img.joyminis.com;
        proxy_set_header Host img.joyminis.com;
        proxy_ssl_name img.joyminis.com;
    }

    # ----------------------------------------------------------
    # 4) API（先救火：允许所有跨域）
    # ----------------------------------------------------------
    location /api/ {
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin '*' always;
            add_header Access-Control-Allow-Methods 'GET, POST, OPTIONS, PUT, DELETE, PATCH' always;
            add_header Access-Control-Allow-Headers '*' always;
            add_header Access-Control-Max-Age 1728000 always;
            add_header Content-Type 'text/plain; charset=utf-8';
            add_header Content-Length 0;
            return 204;
        }

        add_header Access-Control-Allow-Origin '*' always;
        add_header Access-Control-Allow-Headers '*' always;

        proxy_pass http://backend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # ----------------------------------------------------------
    # 5) WebSocket
    # ----------------------------------------------------------
    location ^~ /socket.io/ {
        proxy_pass http://backend:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    # ----------------------------------------------------------
    # 6) Admin 前端
    # ----------------------------------------------------------
    location / {
        proxy_pass http://mini-shop-admin:4000;
        proxy_http_version 1.1;
    }
}

# =========================
# HTTP 80 -> HTTPS
# =========================
server {
    listen 80;
    server_name dev.joyminis.com;
    return 301 https://$host$request_uri;
}