# 环境配置文件，用于 Docker 容器中的开发环境 后端容器运行时用
NODE_ENV=development
# 服务器端口
PORT=3000
# 允许的前端地址（用于 CORS 配置）
CORS_ORIGIN=http://localhost:3000
# 数据库连接字符串
DATABASE_URL=postgresql://dev:devpassword@db:5432/lucky_dev?schema=public
# Redis 连接字符串
REDIS_URL=redis://:strongpass@redis:6379/0
CACHE_TTL=300
CACHE_PREFIX=lucky:

# 开发固定验证码
OTP_DEV_CODE=999999
# 验证码有效期
OTP_TTL_SECONDS=300
# 验证码发送间隔
OTP_INTERVAL_SECONDS=60
# 登录有效期
OTP_LOGIN_WINDOW_SECONDS=300
# 最大尝试次数，防暴力破解
OTP_MAX_ATTEMPTS=5
# 额外的 Pepper 字符串（可选）
OTP_PEPPER=optional_pepper_string

# JWT
JWT_SECRET=please_change_me_very_secret
JWT_EXPIRES_IN=15m

# 允许的前端地址（用于 CORS 配置），多个地址用逗号分隔
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173,http://localhost:64979