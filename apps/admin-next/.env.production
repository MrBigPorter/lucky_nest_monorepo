NEXT_PUBLIC_APP_ENV=production
# API base URL is injected at build time via CI secret (NEXT_PUBLIC_API_BASE_URL)
# Fallback: empty means relative /api (only works with a local proxy)
NEXT_PUBLIC_API_BASE_URL=
